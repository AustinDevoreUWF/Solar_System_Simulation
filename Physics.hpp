#include <cmath>
#include "vector2D.hpp"
class Physics{
    private:
        const double g = 9.8;
        double time=0.0;
        Vector v;
    public:
    double acc(Vector v){};
    double vel(Vector v){};
    double thrust(Vector v){};
    double
};